diff --git a/tools/grammar-sync.py b/tools/grammar-sync.py
index 5d14aad..1f821cc 100644
--- a/tools/grammar-sync.py
+++ b/tools/grammar-sync.py
@@ -18,6 +18,7 @@ import json
 import os
 import plistlib
 import sys
+import urllib.parse
 import urllib.request
 import zipfile
 from pathlib import Path
@@ -152,10 +153,10 @@ def build_contributes(sources, lock):
             if f.get("injectTo"):
                 g["injectTo"] = f["injectTo"]
-            embedded = f.get("embeddedLanguages")
-            if embedded:
-                g["embeddedLanguages"] = embedded
+            for key in GRAMMAR_META:
+                value = f.get(key) or harvested.get(key)
+                if value:
+                    g[key] = value
             grammars.append(g)
     return languages, grammars
