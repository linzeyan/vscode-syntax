{
  "extension.displayName": "Poly",
  "extension.description": "Format and lint on save through the poly daemon -- the same binary and config CI runs -- plus editor conveniences: reference lenses, markdown lists and table of contents, path:line copying.",
  "command.formatDocument.title": "Poly: Format Document (Even When Stopped)",
  "command.formatFile.title": "Poly: Format File",
  "command.formatPath.title": "Poly: Format Folder",
  "command.formatWorkspace.title": "Poly: Format Workspace",
  "command.formatGitRepo.title": "Poly: Format Git Repo",
  "command.formatGitChanged.title": "Poly: Format Git Changed Files",
  "command.lintPath.title": "Poly: Lint (poly check)",
  "command.analyzeDeadCode.title": "Poly: Analyze Dead Code",
  "command.minify.title": "Poly: Minify",
  "command.toggleFormat.title": "Poly: Toggle Formatting",
  "command.toggleLint.title": "Poly: Toggle Linting",
  "command.checkForUpdates.title": "Poly: Check for Updates",
  "command.showOutput.title": "Poly: Show Log",
  "command.createGoWork.title": "Poly: Create go.work for the Open Go Modules",
  "configuration.title": "Poly",
  "config.serverPath.description": "Path to the poly binary. Relative paths resolve against the first workspace folder; empty uses the bundled binary, then `poly` from PATH.",
  "config.lintOnSave.description": "Publish lint diagnostics when a supported file is opened or saved. Takes effect immediately. The Lint switch in the status bar (`Poly: Toggle Linting`) turns this off together with the linters of other installed extensions -- ruff, Go's lint and vet on save, rust-analyzer's check on save, Code Spell Checker, autocorrect, ESLint, ShellCheck, Stylelint, Pylint, Flake8 -- and puts every one of them back as it was when switched on again. Compile and type errors are not lint and stay. Editing this setting directly affects poly alone.",
  "config.format.enabled.description": "Let poly rewrite files: Format Document, format-on-save, Format Selection, and the trailing-whitespace and final-newline fixes .editorconfig asks for on save. The Format switch in the status bar (`Poly: Toggle Formatting`) turns this off together with every automatic rewrite in the editor: format on save, type and paste, code actions on save, trailing whitespace and final newline, including the per-language defaults other extensions ship (golang.go's `[go]` format-on-save, Pylance's `[python]` format-on-type), and puts every one of them back as it was when switched on again. It writes your user settings only; a workspace's own settings that turn formatting on are named in the switch's tooltip rather than changed. Format Document run by hand with another extension's formatter still works. Linting is unaffected. Editing this setting directly affects poly alone. The format shortcut (`Poly: Format Document`, shift+alt+f) formats regardless of this setting and of the switch.",
  "config.deadCodeCodeLens.enabled.description": "Show an `analyze dead code` link on the first line of code in every file poly can answer about: Go (golang.org/x/tools/cmd/deadcode), TypeScript and JavaScript (knip), Python (vulture). Clicking it runs `poly deadcode` in a terminal over that file's whole project — the go.work build list, the npm project, the Python project. Nothing runs until it is clicked: whole-program reachability costs a build, so it is asked rather than watched.",
  "config.languageServers.description": "Run the language server a project's own toolchain provides and route hover, go-to-definition, declaration, type definition, implementation, references, outline, completion, signature help, symbol highlight, folding, expand selection, rename and code actions to it. Nine are known: gopls (Go), rust-analyzer (Rust), clangd (C/C++), sourcekit-lsp (Swift), terraform-ls (Terraform), lua-language-server (Lua), bash-language-server (Shell), buf (Protobuf), arity (R). Code actions arrive as lightbulb actions only — the `source.*` kinds `editor.codeActionsOnSave` runs are left out, because VSCode runs those before the formatter and they would rewrite the same lines poly is about to format. Poly's own lint findings stay: the server's diagnostics are merged with them rather than replacing them, so selene and swiftlint keep reporting. Off by default. A language whose official extension is installed (Go, rust-analyzer, clangd or C/C++, Swift, HashiCorp Terraform, Lua, Bash IDE, Buf) is left to that extension: poly does not start its own server there, so one language never has two running, and installing or removing that extension takes effect without reloading the window. Poly never installs these servers — they come from PATH, and which features work is whatever the server itself declares.",
  "config.languageServerLogs.description": "Forward a downstream language server's own stderr to the Poly output channel. On by default. clangd and terraform-ls write a line per request; gopls, rust-analyzer, sourcekit-lsp and lua-language-server are quiet unless something is wrong. Turning this off discards that output entirely rather than asking each server to be quieter — not all of them can be. Poly's own reports, including a server that is missing or failed to start, are unaffected.",
  "config.memoryLog.description": "Write a line to the Poly output channel every time a file is opened or closed, saying what the daemon is holding: resident memory, how many documents and how many bytes of text, the lint and package caches, the findings each source is keeping, and which language servers are running. Off by default — it is a line per file in a log you also read to find out why a lint did not run. Turn it on when poly's memory looks wrong: the point of the line is that a rising number can be blamed on one of the caches rather than left as \"the daemon got bigger\". Read once at startup, so it takes a window reload.",
  "config.updateCheck.enabled.description": "Periodically check GitHub Releases for new Poly versions. An update installs poly-syntax-highlight too when it is installed, and that extension also checks on its own schedule (poly.syntax.updateCheck.*); a release one of the two already asked about in this window is not asked about again.",
  "config.updateCheck.intervalDays.description": "Days between automatic update checks; 0 checks on every startup.",
  "command.copyPathWithLine.title": "Copy Path with Line Numbers",
  "command.insertTableOfContents.title": "Insert Table of Contents",
  "command.toggleBold.title": "Toggle Bold",
  "command.toggleItalic.title": "Toggle Italic",
  "command.refreshTodos.title": "Refresh TODOs",
  "command.runFile.title": "Run File",
  "command.extractVariable.title": "Extract Variable",
  "command.inlineVariable.title": "Inline Variable",
  "command.moveToNewFile.title": "Move to New File",
  "command.changeSignature.title": "Change Signature",
  "command.implementInterface.title": "Implement Interface",
  "command.nextChangedFile.title": "Go to Next Changed File",
  "command.previousChangedFile.title": "Go to Previous Changed File",
  "command.syntaxColors.title": "Syntax Colors for This Language",
  "command.revertAndSave.title": "Revert Selected Changes and Save",
  "command.continueList.title": "Continue List",
  "command.indentListItem.title": "Indent List Item",
  "command.outdentListItem.title": "Outdent List Item",
  "config.indentTint.enabled.description": "Tint each level of leading whitespace, and mark whitespace that does not fill a level -- a mixed tab/space line shows up as a partial band rather than as nothing. Costs one set of decorations per visible line; nothing is scanned outside the viewport. Colours are the poly.indentLevel1-4 and poly.indentPartial theme colours, each overridable in workbench.colorCustomizations.",
  "config.imagePreview.enabled.description": "Show a thumbnail in the gutter for lines that name an image file which exists. Only lines on screen are resolved, and only paths that resolve to a real file in the workspace; a URL or a missing file draws nothing. Costs one file read per distinct image.",
  "config.unicodeHighlight.enabled.description": "Mark characters that are not what they look like -- invisible and bidirectional controls, spaces that are not U+0020, and lookalikes such as an en dash or a curly quote -- in every file and as you type, the way the gremlins extension does: a mark in the gutter, a tick in the scrollbar, the character's name at the end of its line, and a hover saying why it is marked. Levels follow gremlins, and the colours are the poly.unicodeError, poly.unicodeWarning and poly.unicodeInfo theme colours, each overridable in workbench.colorCustomizations. The Problems entries for the same characters come from the poly/unicode-* rules, which run on save and are what `poly: ignore` and poly.toml silence; this highlight only shows, and neither of them turns it off.",
  "config.referencesCodeLens.enabled.description": "Show a reference count above each declaration, an implementation count above each type and method -- `N impls` over an interface (who satisfies it) and `N interfaces` over a concrete type (what it satisfies) -- and `N methods` over a type whose methods are declared outside it, as Go's are. Every count comes from whatever reference provider the language already has; poly analyses nothing, so a language with no provider gets no lens. Costs one reference query per declaration scrolled into view, and the answer is reused for ten seconds so that typing does not ask again. Clicking any of these lenses goes straight there when there is one place to go, and opens the references list when there are several.",
  "config.protobufCodeLens.enabled.description": "Link a .proto declaration to the Go protoc generated for it: `go type` over a message or enum, `go server` and `go client` over a service, and an implementation count over each rpc. A click goes straight there when there is one place to go and opens the references list when there are several, without leaving the .proto. The lens is drawn only when the generated file is already in the workspace and holds the declaration, so nothing points at a file that has not been generated yet. Requires poly.languageServers, because the .proto outline comes from `buf lsp serve`. Covers protoc-gen-go and protoc-gen-go-grpc; connect-go and other generators name their output in ways the proto does not predict and are left alone.",
  "config.runCodeLens.enabled.description": "Show `run | debug` above a program's entry point -- `main` in Go, Rust, C, C++ and Java, `Main` in C#, `if __name__ == \"__main__\"` in Python, and the shebang of a shell script. `run` executes the file in an integrated terminal; `debug` hands over to whichever debug extension is installed for the language, exactly as F5 would. poly ships no debugger.",
  "config.markdownMermaid.enabled.description": "Render ```mermaid fences as diagrams in the markdown preview. Ignored on VSCode 1.135 and later, which ships the same renderer as a built-in (mermaid-markdown-features): poly stands down rather than draw a second diagram over the first.",
  "config.postfixCompletion.enabled.description": "Offer postfix templates after a dot: `err.if` expands to `if err != nil { }` in Go, `if (err) { }` in TypeScript, `if err:` in Python. Covers go, rust, swift, typescript, javascript (and their react variants), python, lua, c and cpp. These are text templates, not analysis -- poly reads the characters left of the dot and rearranges them -- and they sort after whatever the language server offered, so they never displace a real completion.",
  "config.todo.enabled.description": "Show the TODOs view in the Explorer. The view scans the workspace for the markers in poly.todo.tags, and only while it is on screen -- a closed view costs nothing. Capped at 4000 files and 512KB per file, and it says so when it stops early.",
  "config.todo.tags.description": "Markers the TODOs view lists. Matched as whole words, case-sensitively; entries that are not plain words are ignored.",
  "color.indentLevel1.description": "Background of the first indent level.",
  "color.indentLevel2.description": "Background of the second indent level.",
  "color.indentLevel3.description": "Background of the third indent level.",
  "color.indentLevel4.description": "Background of the fourth indent level; levels beyond it cycle.",
  "color.indentPartial.description": "Background of leading whitespace that does not fill an indent level.",
  "color.unicodeError.description": "Unicode highlight: bidirectional controls and invisible characters, which can change what code means without showing it.",
  "color.unicodeWarning.description": "Unicode highlight: lookalikes such as an en dash or a curly quote, and the few control characters gremlins ranks as warnings.",
  "color.unicodeInfo.description": "Unicode highlight: spaces that are not U+0020, and the soft hyphen.",
  "view.polyTodos.name": "TODOs",
  "view.polyReferences.name": "References",
  "command.category": "Poly"
}
